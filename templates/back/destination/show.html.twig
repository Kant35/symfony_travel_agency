{% extends 'back/layout.html.twig' %}

{% block title %}Destination{% endblock %}

{% block content %}
    <h1>Destination</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ destination.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ destination.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ destination.Description }}</td>
            </tr>
        </tbody>
    </table>
    
    <a href="{{ path('app_destination_edit', {'id': destination.id}) }}" class="btn btn-warning">modifier</a>

    {{ include('back/destination/_delete_form.html.twig') }}
{% endblock %}
